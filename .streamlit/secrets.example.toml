# Reference only. Paste these root-level values into Community Cloud Secrets.
# Never commit secrets.toml or replace this placeholder with a real API key.
APP_EXECUTION_MODE = "direct"
OPENROUTER_API_KEY = "YOUR_OPENROUTER_API_KEY"
OPENROUTER_MODEL = "nex-agi/nex-n2.5-mini:free"
